Creates a template for a new GEdit plugin in interactive mode and starts
gedit to edit those files. Just a shortcut, really.

To run:

$ curl https://github.com/yappie/create-new-gedit-plugin/raw/master/create_plugin.py >\
     tmp_create.py && python tmp_create.py; rm tmp_create.py

